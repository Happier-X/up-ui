import { createApp } from "vue";
import App from "./App.vue";
import "./assets/index.css";

// import UpUi from "../../packages/index";

// createApp(App).use(UpUi).mount("#app");
createApp(App).mount("#app");
